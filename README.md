# colimui

a small terminal ui for colima and docker.

## run

```sh
go run .
```

colimui uses the selected colima profile's docker context. start colima before launching it.

## keys

`enter` expand groups/start-stop services · `t` restart · `d` delete · `l` reload logs · `f` follow logs · `r` refresh · `q` quit
